   

    <div>
    {{#with main}}
    <main>
        <section>
            {{> title data="My educations 126563" big=true}}
        <ul>
            {{#each educations}}
                <li>
                    {{name}}
                    
                    {{#unless isEnd}} (in progress) {{/unless}}
                    
                </li>
            {{/each}}
        </ul>
        </section>
                <section>
            {{> title data="My certificates" big=false}}
         <ul>
            {{#each certificates}}
            <li>
                {{name}} (ID {{id}})                                                   
            </li>
            {{/each}}
         </section>
    </main>
    {{/with}}

</div>